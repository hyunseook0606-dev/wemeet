# ══════════════════════════════════════════════════════════════
# 위밋모빌리티 해상 리스크 플랫폼 — 환경변수 예시
# 사용법: 이 파일을 .env 로 복사 후 실제 키 입력
#
#   Windows PowerShell: Copy-Item .env.example .env
#   Mac/Linux:          cp .env.example .env
#
# 주의: .env 파일은 절대 Git에 커밋하지 마세요 (.gitignore에 등록됨)
# ══════════════════════════════════════════════════════════════


# ── [필수] Google Gemini API ─────────────────────────────────
# 용도: LLM 자동 보고서 생성 (무료, 일 1,500회 한도)
# 발급: https://aistudio.google.com → Get API key
#       구글 계정만 있으면 즉시 발급, 카드 불필요
GEMINI_API_KEY=여기에_Gemini_API_키_입력


# ── [선택] 카카오 API ────────────────────────────────────────
# 용도: 항만 주변 창고·ODCY 실시간 탐색 + 경로 계산
# 발급: https://developers.kakao.com
#   1. 카카오 계정으로 로그인
#   2. 내 애플리케이션 → 애플리케이션 추가하기
#   3. 앱 이름 입력 (예: wemet-platform) → 저장
#   4. 앱 선택 → 앱 키 → REST API 키 복사
# 무료: Local API 300,000건/일, Mobility API 5,000건/일
# 두 변수에 같은 키를 넣으면 됩니다
KAKAO_REST_API_KEY=여기에_카카오_REST_API_키_입력
KAKAO_MOBILITY_KEY=여기에_카카오_REST_API_키_입력   # 위와 동일한 키


# ── [선택] 한국은행 ECOS API ─────────────────────────────────
# 용도: 환율(원/달러), 두바이유, GDP 공식 실데이터
# 발급: https://ecos.bok.or.kr/api/ → 회원가입 → API키 신청
# 없으면 Yahoo Finance(유가), frankfurter.app(환율) 자동 대체
ECOS_API_KEY=여기에_ECOS_API_키_입력


# ── [선택] 부산항만공사 BPA API ──────────────────────────────
# 용도: 부산항 컨테이너 물동량 실데이터 (2020~2024)
# 발급: https://www.data.go.kr → "부산항 컨테이너 수송통계" 검색 → 활용신청
# 없으면 BPA 홈페이지 Excel + 계절분해로 자동 대체
BPA_API_KEY=여기에_BPA_API_키_입력


# ── [선택] Claude (Anthropic) API ────────────────────────────
# 용도: LLM 자동 보고서 (Gemini 없을 때 대안)
# 발급: https://console.anthropic.com/settings/keys
# 유료 (~월 $5), Gemini 무료 키가 있으면 불필요
ANTHROPIC_API_KEY=여기에_Anthropic_API_키_입력


# ── [선택] BigKinds 뉴스빅데이터 API ─────────────────────────
# 용도: 역대 MRI 시계열 (2010~현재) 뉴스 기반 G·D·P 차원 산출
#       historical_mri_builder.py 에서 사용
# 발급: https://www.bigkinds.or.kr
#   1. 회원가입 (무료, 이메일 인증)
#   2. 로그인 → 마이페이지 → Open API 탭
#   3. "API 키 발급" 클릭 → 즉시 발급
# 무료: 일 1,000건 호출 (15년×12월×4쿼리=720건 → 하루에 다 가능)
# 없으면: G·D·P 차원은 시뮬 값으로 대체 (F·V는 실데이터 유지)
BIGKINDS_API_KEY=여기에_BigKinds_API_키_입력


# ── [수동 다운로드] SCFI / CCFI 운임지수 CSV ─────────────────
# 용도: 역대 MRI 시계열 (2010~현재) F 차원 — KCCI가 없는 기간 대체
#       data/freight_index/ 폴더에 저장 후 자동 인식
#
# SCFI (상하이컨테이너운임지수, 2009년 10월~):
#   공식: https://en.sse.net.cn/indices/scfinew.jsp
#   무료: https://en.macromicro.me/series/7541/china-scfi (CSV 다운로드)
#   저장명: data/freight_index/scfi.csv  (컬럼: Date, Close 또는 날짜, 지수)
#
# CCFI (중국컨테이너운임지수, 1998년~):
#   공식: https://en.sse.net.cn/indices/ccfinew.jsp
#   무료: https://en.macromicro.me/series/8003/china-ccfi (CSV 다운로드)
#   저장명: data/freight_index/ccfi.csv
#
# 참고: KCCI는 2022년 11월부터 자동 반영 (기존 XLS 파일 그대로 사용)
#       2022-11 이후: KCCI 70% + SCFI 30% 혼합
#       2022-10 이전: SCFI 70% + CCFI 30% 혼합


# ── [선택] 부산항 물동량 과거 데이터 (2010~2019) ─────────────
# 용도: 역대 MRI V 차원 — BPA API가 2020년부터만 제공되므로 보완용
# 다운로드: https://www.data.go.kr/data/15055477/fileData.do
#   (부산항만공사_부산항 연도별 물동량 추이 — 1993년~)
# 저장명: data/busan_throughput_2010.csv  (컬럼: 년월, TEU)


# ── [참고] 공공데이터포털 KCCI API ───────────────────────────
# 용도: KCCI 운임지수 실시간 API (선택 — XLS 파일로도 대체 가능)
# 발급: https://www.data.go.kr/data/15131881/openapi.do
# DATA_GO_KR_KEY=여기에_공공데이터포털_키_입력
